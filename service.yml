apiVersion: v1
kind: Service
Metadata:
 name: clusterip
spec:
  type: clusterip
  selector:
    app: new-app
  ports:
    - port: 80
      targetPort: 80
      name: nginx 
      protocol: TCP
    - port: 8080
      targetPort: 8080
      name: tomcat
      protocol: TCP
    